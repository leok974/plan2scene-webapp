# Execution Mode
# Options: demo (simulated pipeline) or gpu (real Plan2Scene execution)
MODE=demo

# Pipeline Mode (for MODE=gpu only)
# Options: preprocessed (existing behavior, requires preprocessed Rent3D++ data)
#          full (runs complete pipeline from R2V vectors)
PIPELINE_MODE=preprocessed

# Local storage directories
UPLOAD_DIR=uploads
JOBS_DIR=static/jobs

# Plan2Scene Repository Configuration
# Path to the cloned Plan2Scene repository
PLAN2SCENE_ROOT=../plan2scene

# Optional: Override Plan2Scene data directory
# If not set, defaults to ${PLAN2SCENE_ROOT}/data
# PLAN2SCENE_DATA_ROOT=/custom/path/to/plan2scene/data

# R2V-to-Plan2Scene Repository
# Path to the r2v-to-plan2scene converter repository
R2V_TO_PLAN2SCENE_ROOT=../r2v-to-plan2scene

# Raster-to-Vector Repository (Optional, for future integration)
# Path to the raster-to-vector (R2V) repository
RASTER_TO_VECTOR_ROOT=../raster-to-vector

# GPU Availability / CPU Fallback
# Set to 0 or false to force CPU mode if your GPU is too new for the PyTorch build
# (e.g., RTX 5070 Ti with sm_120 on PyTorch 2.0.1 which only supports up to sm_86)
# CPU mode will be slower but the full pipeline still runs end-to-end.
PLAN2SCENE_GPU_ENABLED=true
